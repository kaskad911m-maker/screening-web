# Домашнее задание — модуль 4, урок 3 (платформа: обратная связь + фундамент бэкенда)

## Текст сообщения куратору (скопируй и вставь)

---

Здравствуйте!

Домашнее задание к **уроку 3 (модуль 4)** по пунктам с платформы:

1. **Доработка приложения по обратной связи (минимум 2–3 точечных правки)**  
   - **`mini-app.html`** — заявка «Записаться на скрининг»: проверка, что расчёт по дате уже сделан (иначе понятное сообщение и без пустого запроса к API); тактильная обратная связь в Telegram Mini App при успехе/ошибке; прокрутка к статусу при ошибке; у блока статуса `tabindex` для удобства фокуса.  
   - **`index.html`** — форма заявки на лендинге: защита от повторного клика «Отправить» во время отправки; фокус на первое незаполненное поле при ошибке валидации; прокрутка к сообщению об ошибке сети.

2. **Шаг 0 и шаг 1 из блока 3**  
   - План: **`BACKEND-PLAN.md`** (сущности, API, этапы, Supabase).  
   - База в Supabase: по инструкции **`SUPABASE-ШАГ1.md`** выполнен скрипт **`supabase/schema.sql`** — в Table Editor таблицы `masters`, `bookings`, `leads`, в `masters` есть строка `marina`.

3. **`DEPLOYMENT.md`** — готовая пошаговая инструкция (локальный запуск, Vercel, GitHub Pages, Supabase, переменные backend, чек-листы).

Урок на платформе про ИИ-агента: бот-копирайтер в репозитории в папке **`copywriter-bot/`** (отдельный сервис на Railway при необходимости).

Репозиторий: **`screening-web`** / ссылка: ___

---

## Как проверить кратко

| Пункт | Действие |
|--------|----------|
| Обратная связь Mini App | Backend + `mini-app.html?api=…` → расчёт → «Записаться»; без расчёта — предупреждение без POST. |
| Форма на сайте | `index.html` → пустые поля / двойной клик / обрыв сети — ожидаемое поведение выше. |
| Шаг 0–1 | Открыть **`BACKEND-PLAN.md`**, **`supabase/schema.sql`**; в Supabase студента — таблицы на месте. |
| Деплой | Прочитать **`DEPLOYMENT.md`** с начала — связность шагов. |
